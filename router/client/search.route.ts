import * as searchControllers from "../../controllers/client/search.controllers";
import express from "express";

const router = express.Router();

// router.get("/:type", searchControllers.search);

export const routerSearch = router;
